# Readiness successor cold installation and operator procedure

This is local delivery and operator procedure evidence. It does not accept any
mandatory agent host, publish an artifact, or transfer an earlier host result.
The local bundle was assembled from clean public harness source
`d1bc64428bd30f979ed23cb454dabfbe6729b1b8`. Its archive SHA256 is
`3779a0bdc5f705516a100a2b2ba7dde85debc7eeed0f923eb0839b390e729e55`,
518,088,302 bytes. `identity.json.gz` records the exact source bundle, archive,
cold extraction and kernel identities. All 51 archive files were reread, and
system tar extraction into a fresh private directory preserved the selected
modes. The cold verifier checked all files and OCI blobs; the cold binary
reported `chio-cli 0.1.1-rc.1`. No private source checkout is a runtime dependency
of that extracted installation.

This successor selects the bound readiness helper and resource-owner README from
Chio source `3374dba0c1537e933e8f3e1074264b96668e5825`. The helper SHA256 is
`efb4cbde3d032a461d804dc40e2bae32e179219f95ecddf0d112e37cfa14dd6c`.
All selected host/bridge/image/kernel artifact bytes remain unchanged from the
initial static local bundle. The installed bundle manifest, checksum inventory
and operator instructions are retained losslessly here.

An empty-cache offline installation of the selected operator bridge matched all
1,056 regular archive files. The exact cold resource-owner helper started a new
dedicated owner on loopback port 59212 in 1.65 seconds. The next command invoked
the repaired preparation helper immediately and succeeded in 0.98 seconds,
creating exactly one session directory without retry. The signer already
existed when this invocation began. This actual observation verifies the bound
cold path; delayed-key waiting and timeout behavior have separate focused tests
in the owning Chio readiness record. Neither observation claims host acceptance.

A verified operator gateway write produced exactly one independently observed
filesystem dispatch and the expected bytes. The revocation snippet was extracted
unchanged from this installed bundle's README and executed using its original
private operator and gateway files. Its actual responses bound revocation to the
original delegated session credential and capability. The next write returned
`not_dispatched`; the independently observed resource/audit state was identical,
with the forbidden file absent and zero new dispatches. The owner then stopped
successfully, retaining its authority, databases, gateway journal and both
resource/audit volumes. The exact new state paths and commands are in the raw
records. Provider credentials and private authority/configuration files are not
included.

The 46 raw files are losslessly compressed. `raw-files.json` binds each original
byte stream and compressed file. Earlier startup, signer-readiness and extraction
failures remain in the separate `static-local` record. A later operator-runbook
addition describes the separately observed session-idle-expiry limit and requires
a distinct documentation successor; it does not rewrite these exact tested
instructions or infer a native host result from this procedure.
