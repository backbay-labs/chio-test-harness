# Final local documentation successor

This is an unaccepted, unpublished local candidate. The archive is
`required-agents-static-rc-local-20260910-r3.tar.gz`, SHA256
`25750f19f27ad49793df9a0ae72ab55b920ffb544cb0fa426e9e80ee453de5b9`,
518,088,554 bytes. The assembly source is
`9c2b5c0d1ab433f188a5519a8d0f304503f4f277`.
The exact archive, source bundle and cold-installation paths are recorded in
`identity.json.gz`. The source bundle is an assembly input only; no private
sibling checkout is a runtime dependency of the cold-extracted installation.

All 48 selected inputs plus the standalone verifier retain explicit identities.
The archive's 51 files were reread against their checksums. System tar extraction
into a new private root preserved modes; its installed verifier passed complete
file inventory and OCI blob checks, and its kernel reported
`chio-cli 0.1.1-rc.1`. No Docker build, image export, native host launch or active
resource owner was needed for this documentation successor.

A complete manifest comparison with the independently tested predecessor archive
`3779a0bdc5f705516a100a2b2ba7dde85debc7eeed0f923eb0839b390e729e55`
found exactly one changed selected file: `README.md`. The other 48 entries,
including the kernel, helper, all packages, wheelhouse and both OCI image archives,
are byte-identical. The administrative revocation snippet also remains identical,
SHA256 `ae7c28dfa2c752c672d811caea2cd5c1de95b0aaa0bae5537bc9ed6267c11bd7`.
The runbook adds the separately observed limit that session idle expiry may
precede delegated credential expiry. Original expired session state must remain
retained; a fresh session cannot silently substitute for its recovery.

The exact prior cold owner, one-shot preparation, useful operator write,
credential/capability revocations, independent no-new-effect observation and
owner shutdown remain in `../static-successor/`. This directory verifies the
new documentation assembly and byte comparisons. It does not invent a native
host run for this archive, transfer host acceptance, or qualify the eventual
hosted release binary. All six acceptance fields and publication remain
unresolved in the manifest. Public installation must later verify the actual
released assets, provenance and per-host installation/lifecycle gates.

The 16 raw files are retained losslessly with original and compressed hashes in
`raw-files.json`. Private input-root maps, owner/session configuration, secrets,
profiles, databases, journals, installation trees and resource volumes are not
included in this evidence or delivery bundle.
